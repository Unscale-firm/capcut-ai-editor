# QUESTIONS — editor ↔ Unscale (append-only; Unscale answers inline)

## 2026-07-15 — editor, after cutting Increments 1–2

1. **Hospital photo (2.4 + 2.7) — REQUIRED.** Increment 2 is cut with the PiP slot
   reserved at "two weeks on a hospital bed" (03:24 area) and the 1s callback at 04:23.
   Photo drops in as soon as it lands in `B-roll assets/increment-2_story+layoffs/`.

2. **Unscale wordmark.** Name card at 00:43 currently uses the favicon placeholder
   (`unscale_icon_192.png`) next to the McKinsey/Forbes logos. Please add the full
   wordmark (white/mono on transparent) from the brand kit.

3. **Oracle layoff figure — map vs audio mismatch.** The map (2.6) says chyron
   **ORACLE — 21,000**, but the spoken line + burned caption at ~04:05 read
   **"Oracle, 20,000"**. Chyron currently follows the map (21,000). Which figure is
   right? Source screenshot to E3.25 either way; if 20,000 is correct we flip the card.

4. **Renders.** Increments 1 and 2 are cut (preview files local to the edit machine).
   Cold-open reaction tiles are small sources (Kully is 324×246) — shown as centered
   tiles on Dark Ops, not full-frame blowups. Where do you want render links shared
   (Drive folder / WeTransfer / other)?
